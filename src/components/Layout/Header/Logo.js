import classes from './Logo.module.scss';

const Logo = () => {
  return (
    <div id="logo" className={classes.logo}>
      <a href=".">Mac's Collection</a>
    </div>
  );
};

export default Logo;
